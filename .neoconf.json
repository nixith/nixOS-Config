{
  "lspconfig": {
    "nixd": {
      "nixd": {
        "nixpkgs": {
          "expr": "(import ./npins).nixpkgs"
        },
        "formatting": {
          "command": [
            "nixfmt"
          ]
        },
        "options": {
          "nixos": {
            "expr": "(import <nixos-system>).options"
          },
          "home": {
            "expr": "(import <nixos-system>).options.home-manager.users.type.getSubOptions []"
          }
        }
      }
    }
  }
}
